# 在线多人聊天室修改文件介绍

## 概述

本文档详细介绍了为在线多人聊天室添加私有聊天室功能和菜单功能所做的修改。

## 修改的文件

### 1. 服务器端文件：ss.cpp

#### 主要修改内容：

##### 1.1 新增头文件
- 添加了 `<map>`、`<set>`、`<sstream>` 头文件，用于实现私有聊天室的管理和命令解析。

##### 1.2 扩展连接结构体 (CON)
- 新增 `name` 字段：存储用户昵称
- 新增 `currentRoom` 字段：存储用户当前所在的聊天室ID

##### 1.3 新增私有聊天室类 (PrivateRoom)
- `roomId`：4位数字的房间唯一标识
- `members`：存储房间成员的socket集合

##### 1.4 新增全局变量
- `privateRooms`：map类型，存储所有私有聊天室，key为房间号，value为PrivateRoom对象
- `socketToName`：map类型，存储socket与用户名的映射关系

##### 1.5 新增辅助函数
- `generateRoomId()`：生成唯一的4位数字房间号
- `sendToClient()`：向指定客户端发送消息
- `broadcastToRoom()`：向指定聊天室的所有成员广播消息

##### 1.6 新增命令处理函数 (handleCommand)
处理以下命令：
- `/create`：创建私有聊天室
- `/join <房间号>`：加入指定的私有聊天室
- `/leave`：离开当前私有聊天室，回到公共聊天室
- `/list`：查看所有私有聊天室列表
- `/help`：显示帮助信息
- `/menu`：显示菜单

##### 1.7 修改消息处理逻辑
- 客户端连接后，首先接收用户名
- 区分普通消息和命令消息（以/开头）
- 根据用户当前所在的聊天室进行消息广播
- 用户断开连接时，从所在聊天室移除

### 2. 客户端文件：cc.cpp

#### 主要修改内容：

##### 2.1 新增成员变量
- `nameSent`：标记用户名是否已发送到服务器

##### 2.2 新增菜单显示函数 (showMenu)
显示以下菜单选项：
1. 创建私有聊天室
2. 加入私有聊天室
3. 离开私有聊天室
4. 查看所有私有聊天室
5. 显示帮助信息
6. 发送消息
7. 退出聊天室

##### 2.3 新增菜单选择处理函数 (handleMenuChoice)
根据用户选择执行相应操作：
- 选项1：发送 `/create` 命令
- 选项2：提示输入房间号，发送 `/join <房间号>` 命令
- 选项3：发送 `/leave` 命令
- 选项4：发送 `/list` 命令
- 选项5：发送 `/help` 命令
- 选项6：提示输入消息内容并发送
- 选项7：退出聊天室

##### 2.4 修改发送数据函数 (sendData)
- 连接成功后首先发送用户名
- 循环显示菜单并处理用户选择

## 新增功能说明

### 1. 私有聊天室功能
- **创建**：用户可通过菜单或命令创建私有聊天室，系统自动生成4位唯一房间号
- **加入**：用户可通过输入4位房间号加入已存在的私有聊天室
- **离开**：用户可离开私有聊天室，回到公共聊天室
- **消息隔离**：私有聊天室中的消息仅对房间内成员可见
- **自动解散**：当私有聊天室中没有成员时，房间自动解散

### 2. 菜单功能
- **直观界面**：提供清晰的菜单选项，方便用户操作
- **操作引导**：通过菜单引导用户完成各种操作
- **命令支持**：同时支持命令行方式和菜单方式操作

## 技术实现细节

### 1. 房间号生成
- 使用随机数生成4位数字
- 检查是否已存在，确保唯一性
- 范围：0000-9999

### 2. 消息广播机制
- 公共聊天室：向所有在公共聊天室的用户广播
- 私有聊天室：仅向房间内成员广播
- 系统消息：如用户加入/离开通知，也按聊天室隔离

### 3. 用户状态管理
- 每个用户有当前所在聊天室状态
- 加入/离开时更新状态
- 断开连接时自动清理

## 编译和运行

### 编译命令
```bash
# 编译服务器端
g++ -o ss.exe ss.cpp -lws2_32 -std=c++11

# 编译客户端
g++ -o cc.exe cc.cpp -lws2_32 -std=c++11
```

### 运行步骤
1. 先运行服务器端：`ss.exe`，输入端口号
2. 再运行客户端：`cc.exe`，输入昵称、服务器IP和端口号
3. 按照菜单提示进行操作

## 总结

本次修改成功实现了以下功能：
1. 私有聊天室的创建、加入、离开功能
2. 4位唯一房间号的生成和管理
3. 消息按聊天室隔离的广播机制
4. 直观的菜单操作界面
5. 命令行和菜单两种操作方式

这些修改使得聊天室功能更加丰富，用户体验更加友好，同时保持了代码的可维护性和扩展性。
