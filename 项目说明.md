# 网络聊天室项目

## 项目概述

这是一个基于TCP/IP协议的网络聊天室应用，支持多用户实时聊天、私有聊天室创建与管理等功能。项目包含两种实现版本：

1. **C++命令行版本** - 原始实现，使用Windows Socket API
2. **Python GUI版本** - 新增实现，使用CustomTkinter提供现代化图形界面

### 主要功能

- **公共聊天室** - 所有用户可见的公共聊天区域，即使用户在私有聊天室中也能看到公共消息
- **私有聊天室** - 用户可创建4位数字房间号的私有聊天室，消息仅房间内成员可见
- **双区域消息展示** - 公共聊天室和私有聊天室消息分离显示，一目了然
- **实时消息** - 支持多用户同时在线，实时收发消息
- **用户管理** - 服务器端可查看在线用户和聊天室状态
- **命令系统** - 支持多种命令操作（创建/加入/离开房间等）

### 技术栈

#### C++版本
- **编程语言**: C++
- **网络库**: Windows Socket API (Winsock2)
- **线程模型**: 多线程（每个客户端一个线程）
- **编译器**: MinGW 或 Visual Studio

#### Python GUI版本
- **编程语言**: Python 3.x
- **GUI框架**: CustomTkinter (基于Tkinter的现代化UI库)
- **网络库**: socket (Python标准库)
- **线程模型**: 多线程（网络通信在后台线程运行）

## 项目结构

```
network_hw-seed/
├── ss.cpp              # C++服务器端源码
├── ss.exe              # C++服务器端可执行文件
├── cc.cpp              # C++客户端源码
├── cc.exe              # C++客户端可执行文件
├── server_ui.py        # Python GUI服务器端
├── client_ui.py        # Python GUI客户端端
├── 项目说明.md          # 项目说明文档
├── README.md           # 项目说明文档
├── 用户操作手册.md      # 用户操作手册
├── 修改文件介绍.md      # 修改文件介绍
├── Bug修复分析.md       # Bug修复分析
└── Bug修复分析2.0.md    # Bug修复分析2.0
```

## 环境要求

### Python GUI版本
- Python 3.7 或更高版本
- CustomTkinter 库

### C++版本
- Windows 操作系统
- MinGW 编译器 或 Visual Studio
- Winsock2 库（Windows系统自带）

## 安装步骤

### 1. 安装Python（如使用GUI版本）

从 [Python官网](https://www.python.org/downloads/) 下载并安装Python 3.7+。

### 2. 安装CustomTkinter库

打开命令行，执行以下命令：

```bash
pip install customtkinter
```

或者使用国内镜像源：

```bash
pip install customtkinter -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 运行说明

### 方式一：使用Python GUI版本（推荐）

#### 步骤1：启动服务器

1. 打开命令行窗口
2. 进入项目目录：
   ```bash
   cd "c:\Users\Ha ha\Desktop\A\network_hw-seed"
   ```
3. 运行服务器程序：
   ```bash
   python server_ui.py
   ```
4. 在GUI界面中：
   - 设置端口号（默认8888）
   - 点击"启动服务器"按钮

#### 步骤2：启动客户端

1. 打开新的命令行窗口
2. 进入项目目录
3. 运行客户端程序：
   ```bash
   python client_ui.py
   ```
4. 在登录界面中：
   - 输入昵称
   - 输入服务器IP地址（本地测试用127.0.0.1）
   - 输入端口号（与服务器一致）
   - 点击"连接服务器"按钮

#### 步骤3：开始聊天

连接成功后，聊天界面分为两个消息区域：

**上方区域 - 公共聊天室（📢 蓝色标签）**
- 显示所有用户发送的公共消息
- **所有人可见**，即使用户在私有聊天室中也能看到
- 消息格式：`[公共] 用户名 说: 消息内容`

**下方区域 - 当前房间（💬 绿色标签）**
- 显示当前所在房间的消息
- 如果在公共聊天室：显示公共消息（与上方区域同步）
- 如果在私有聊天室：仅显示该私有房间内的消息
- 消息格式：`[私有] 用户名 说: 消息内容`

**消息发送规则**：
- 在公共聊天室时：发送的消息会广播给所有用户，显示在所有人的公共聊天室区域
- 在私有聊天室时：发送的消息仅房间内成员可见，显示在房间成员的当前房间区域

## GUI客户端界面说明

### 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  📍 当前位置: 公共聊天室                    👤 用户名        │
├─────────────────────────────────────────────────────────────┤
│  📢 公共聊天室 (所有人可见)                          [蓝色]  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ [公共] 用户A 说: 大家好！                           │   │
│  │ [公共] 用户B 说: 你好！                             │   │
│  │ ...                                                  │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  💬 当前房间: 公共聊天室                            [绿色]  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ [公共] 用户A 说: 大家好！                           │   │
│  │ [公共] 用户B 说: 你好！                             │   │
│  │ ...                                                  │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  [输入消息... (按回车发送)]                    [发送按钮]   │
└─────────────────────────────────────────────────────────────┘
```

### 右侧功能菜单

- **🏠 创建私有聊天室** - 创建一个新的私有聊天室，系统自动生成4位房间号
- **🚪 加入私有聊天室** - 输入4位房间号加入已有的私有聊天室
- **↩️ 离开私有聊天室** - 离开当前私有聊天室，回到公共聊天室
- **📋 查看所有聊天室** - 查看当前所有活跃的私有聊天室列表
- **❓ 帮助信息** - 显示可用命令和操作说明
- **🚪 退出聊天室** - 断开连接并退出程序

### 消息区分说明

| 消息前缀 | 来源 | 可见范围 | 显示区域 |
|---------|------|---------|---------|
| `[公共]` | 公共聊天室发送 | 所有用户 | 上方公共区域 + 下方当前区域（如果在公共聊天室） |
| `[私有]` | 私有聊天室发送 | 仅房间成员 | 下方当前区域（房间成员） |
| `[系统]` | 系统通知 | 目标用户 | 右侧系统消息区域 |

## 功能使用指南

### 创建私有聊天室

1. **GUI版本**: 点击右侧"创建私有聊天室"按钮
2. **命令行版本**: 选择菜单选项1，或输入 `/create` 命令

系统会自动生成一个4位数字的房间号，其他用户可通过此房间号加入。

**注意**：创建私有聊天室后：
- 您将自动进入该私有聊天室
- 公共聊天室的消息仍然可以在上方区域看到
- 您发送的消息将仅对房间内成员可见

### 加入私有聊天室

1. **GUI版本**: 点击"加入私有聊天室"按钮，输入4位房间号
2. **命令行版本**: 选择菜单选项2，或输入 `/join <房间号>` 命令

**注意**：加入私有聊天室后：
- 上方区域仍然显示所有公共消息
- 下方区域显示该私有房间的消息
- 您发送的消息仅房间内成员可见

### 离开私有聊天室

1. **GUI版本**: 点击"离开私有聊天室"按钮
2. **命令行版本**: 选择菜单选项3，或输入 `/leave` 命令

离开后将回到公共聊天室，此时：
- 上方区域和下方区域都显示公共消息
- 您发送的消息将广播给所有用户

### 查看所有聊天室

1. **GUI版本**: 点击"查看所有聊天室"按钮
2. **命令行版本**: 选择菜单选项4，或输入 `/list` 命令

### 查看帮助信息

1. **GUI版本**: 点击"帮助信息"按钮
2. **命令行版本**: 选择菜单选项5，或输入 `/help` 命令

## 多用户测试

要测试多用户聊天功能：

1. 启动一个服务器实例
2. 启动多个客户端实例（每个客户端需要独立的命令行窗口）
3. 不同客户端之间可以互相收发消息
4. 可以创建私有聊天室进行私密聊天

**测试场景示例**：

1. **用户A** 和 **用户B** 都在公共聊天室
   - 双方发送的消息都显示在对方的公共区域和当前区域

2. **用户A** 创建私有聊天室（房间号：1234）
   - 用户A的当前区域变为私有聊天室
   - 用户A仍然可以看到公共消息
   - 用户B看不到用户A的私有消息

3. **用户B** 加入房间 1234
   - 用户A和用户B可以在私有聊天室中聊天
   - 双方仍然可以看到公共聊天室的消息
   - 其他用户看不到他们的私有对话

## 编译C++版本（可选）

如果需要重新编译C++源码：

### 使用MinGW编译

```bash
# 编译服务器
g++ ss.cpp -o ss.exe -lws2_32 -std=c++11

# 编译客户端
g++ cc.cpp -o cc.exe -lws2_32 -std=c++11
```

### 使用Visual Studio编译

1. 打开Visual Studio Developer Command Prompt
2. 执行：
   ```bash
   cl ss.cpp ws2_32.lib
   cl cc.cpp ws2_32.lib
   ```

## 注意事项

1. **防火墙设置**: 首次运行时，Windows防火墙可能会提示是否允许网络访问，请选择"允许"。

2. **端口占用**: 确保使用的端口号未被其他程序占用。如果端口被占用，可以尝试使用其他端口（如8889、9999等）。

3. **编码问题**: 项目使用GBK编码，在非中文Windows系统上可能会出现乱码。

4. **多线程安全**: Python GUI版本使用了多线程，确保网络操作不会阻塞UI界面。

5. **消息格式**: 公共消息和私有消息通过前缀区分，客户端根据前缀将消息显示在不同区域。

## 常见问题

### Q: 无法连接服务器？
A: 请检查：
- 服务器是否已启动
- IP地址和端口号是否正确
- 防火墙是否阻止了连接
- 服务器和客户端是否在同一网络（或使用正确的公网IP）

### Q: 公共消息和私有消息有什么区别？
A: 
- **公共消息**: 发送给所有用户，显示在所有人的公共聊天室区域
- **私有消息**: 仅发送给房间内成员，只显示在房间成员的当前房间区域
- **关键区别**: 即使用户在私有聊天室中，也能看到公共消息

### Q: 如何知道我当前在哪个房间？
A: 
- 界面顶部显示"📍 当前位置: XXX"
- 下方消息区域的标签显示"💬 当前房间: XXX"
- 公共聊天室时，两个区域显示相同的消息
- 私有聊天室时，上方显示公共消息，下方显示私有消息

### Q: CustomTkinter安装失败？
A: 尝试：
- 升级pip: `python -m pip install --upgrade pip`
- 使用国内镜像源: `pip install customtkinter -i https://pypi.tuna.tsinghua.edu.cn/simple`

## 版本历史

- **v1.0**: 初始C++命令行版本
- **v2.0**: 新增Python GUI版本，提供现代化图形界面
- **v2.1**: 分离公共聊天室和私有聊天室的消息展示，公共消息对所有用户可见

## 许可证

本项目仅供学习交流使用。
