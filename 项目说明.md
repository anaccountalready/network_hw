# 网络聊天室项目

## 项目概述

这是一个基于TCP/IP协议的网络聊天室应用，支持多用户实时聊天、私有聊天室创建与管理等功能。项目包含两种实现版本：

1. **C++命令行版本** - 原始实现，使用Windows Socket API
2. **Python GUI版本** - 新增实现，使用CustomTkinter提供现代化图形界面

### 主要功能

- **公共聊天室** - 所有用户默认加入的公共聊天区域
- **私有聊天室** - 用户可创建4位数字房间号的私有聊天室
- **实时消息** - 支持多用户同时在线，实时收发消息
- **用户管理** - 服务器端可查看在线用户和聊天室状态
- **命令系统** - 支持多种命令操作（创建/加入/离开房间等）

### 技术栈

#### C++版本
- **编程语言**: C++
- **网络库**: Windows Socket API (Winsock2)
- **线程模型**: 多线程（每个客户端一个线程）
- **编译器**: MinGW 或 Visual Studio

#### Python GUI版本
- **编程语言**: Python 3.x
- **GUI框架**: CustomTkinter (基于Tkinter的现代化UI库)
- **网络库**: socket (Python标准库)
- **线程模型**: 多线程（网络通信在后台线程运行）

## 项目结构

```
network_hw-seed/
├── ss.cpp              # C++服务器端源码
├── ss.exe              # C++服务器端可执行文件
├── cc.cpp              # C++客户端源码
├── cc.exe              # C++客户端可执行文件
├── server_ui.py        # Python GUI服务器端
├── client_ui.py        # Python GUI客户端端
├── README.md           # 项目说明文档
├── 用户操作手册.md      # 用户操作手册
├── 修改文件介绍.md      # 修改文件介绍
├── Bug修复分析.md       # Bug修复分析
└── Bug修复分析2.0.md    # Bug修复分析2.0
```

## 环境要求

### Python GUI版本
- Python 3.7 或更高版本
- CustomTkinter 库

### C++版本
- Windows 操作系统
- MinGW 编译器 或 Visual Studio
- Winsock2 库（Windows系统自带）

## 安装步骤

### 1. 安装Python（如使用GUI版本）

从 [Python官网](https://www.python.org/downloads/) 下载并安装Python 3.7+。

### 2. 安装CustomTkinter库

打开命令行，执行以下命令：

```bash
pip install customtkinter
```

或者使用国内镜像源：

```bash
pip install customtkinter -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 运行说明

### 方式一：使用Python GUI版本（推荐）

#### 步骤1：启动服务器

1. 打开命令行窗口
2. 进入项目目录：
   ```bash
   cd "c:\Users\Ha ha\Desktop\A\network_hw-seed"
   ```
3. 运行服务器程序：
   ```bash
   python server_ui.py
   ```
4. 在GUI界面中：
   - 设置端口号（默认8888）
   - 点击"启动服务器"按钮

#### 步骤2：启动客户端

1. 打开新的命令行窗口
2. 进入项目目录
3. 运行客户端程序：
   ```bash
   python client_ui.py
   ```
4. 在登录界面中：
   - 输入昵称
   - 输入服务器IP地址（本地测试用127.0.0.1）
   - 输入端口号（与服务器一致）
   - 点击"连接服务器"按钮

#### 步骤3：开始聊天

连接成功后，您可以：
- 在消息输入框中输入文字，按回车或点击"发送"按钮发送消息
- 使用右侧功能菜单创建/加入私有聊天室
- 查看系统消息和在线用户

### 方式二：使用C++命令行版本

#### 步骤1：启动服务器

1. 双击运行 `ss.exe`
2. 输入端口号（如8888）
3. 等待客户端连接

#### 步骤2：启动客户端

1. 双击运行 `cc.exe`
2. 输入昵称
3. 输入服务器IP地址（本地测试用127.0.0.1）
4. 输入端口号（与服务器一致）

#### 步骤3：使用菜单操作

客户端提供以下菜单选项：
1. 创建私有聊天室
2. 加入私有聊天室
3. 离开私有聊天室
4. 查看所有私有聊天室
5. 显示帮助信息
6. 发送消息
7. 退出聊天室

## 功能使用指南

### 创建私有聊天室

1. **GUI版本**: 点击右侧"创建私有聊天室"按钮
2. **命令行版本**: 选择菜单选项1，或输入 `/create` 命令

系统会自动生成一个4位数字的房间号，其他用户可通过此房间号加入。

### 加入私有聊天室

1. **GUI版本**: 点击"加入私有聊天室"按钮，输入4位房间号
2. **命令行版本**: 选择菜单选项2，或输入 `/join <房间号>` 命令

### 离开私有聊天室

1. **GUI版本**: 点击"离开私有聊天室"按钮
2. **命令行版本**: 选择菜单选项3，或输入 `/leave` 命令

离开后将回到公共聊天室。

### 查看所有聊天室

1. **GUI版本**: 点击"查看所有聊天室"按钮
2. **命令行版本**: 选择菜单选项4，或输入 `/list` 命令

### 查看帮助信息

1. **GUI版本**: 点击"帮助信息"按钮
2. **命令行版本**: 选择菜单选项5，或输入 `/help` 命令

## 多用户测试

要测试多用户聊天功能：

1. 启动一个服务器实例
2. 启动多个客户端实例（每个客户端需要独立的命令行窗口）
3. 不同客户端之间可以互相收发消息
4. 可以创建私有聊天室进行私密聊天

## 编译C++版本（可选）

如果需要重新编译C++源码：

### 使用MinGW编译

```bash
# 编译服务器
g++ ss.cpp -o ss.exe -lws2_32 -std=c++11

# 编译客户端
g++ cc.cpp -o cc.exe -lws2_32 -std=c++11
```

### 使用Visual Studio编译

1. 打开Visual Studio Developer Command Prompt
2. 执行：
   ```bash
   cl ss.cpp ws2_32.lib
   cl cc.cpp ws2_32.lib
   ```

## 注意事项

1. **防火墙设置**: 首次运行时，Windows防火墙可能会提示是否允许网络访问，请选择"允许"。

2. **端口占用**: 确保使用的端口号未被其他程序占用。如果端口被占用，可以尝试使用其他端口（如8889、9999等）。

3. **编码问题**: 项目使用GBK编码，在非中文Windows系统上可能会出现乱码。

4. **多线程安全**: Python GUI版本使用了多线程，确保网络操作不会阻塞UI界面。

## 常见问题

### Q: 无法连接服务器？
A: 请检查：
- 服务器是否已启动
- IP地址和端口号是否正确
- 防火墙是否阻止了连接
- 服务器和客户端是否在同一网络（或使用正确的公网IP）

### Q: 消息发送后没有显示？
A: 请检查：
- 网络连接是否正常
- 消息是否为空
- 其他用户是否在同一聊天室

### Q: CustomTkinter安装失败？
A: 尝试：
- 升级pip: `python -m pip install --upgrade pip`
- 使用国内镜像源: `pip install customtkinter -i https://pypi.tuna.tsinghua.edu.cn/simple`

## 版本历史

- **v1.0**: 初始C++命令行版本
- **v2.0**: 新增Python GUI版本，提供现代化图形界面

## 许可证

本项目仅供学习交流使用。
