# 在线多人聊天室用户操作手册

## 目录
1. [系统概述](#1-系统概述)
2. [环境要求](#2-环境要求)
3. [编译和运行](#3-编译和运行)
4. [连接服务器](#4-连接服务器)
5. [菜单操作指南](#5-菜单操作指南)
6. [命令行操作指南](#6-命令行操作指南)
7. [功能详细说明](#7-功能详细说明)
8. [常见问题](#8-常见问题)

---

## 1. 系统概述

本系统是一个基于TCP/IP协议的在线多人聊天室，支持以下功能：
- **公共聊天室**：所有连接到服务器的用户都可以在公共聊天室中交流
- **私有聊天室**：用户可以创建或加入私有聊天室，进行私密交流
- **菜单操作**：提供直观的菜单界面，方便用户操作
- **命令行操作**：同时支持命令行方式，适合高级用户

---

## 2. 环境要求

### 2.1 操作系统
- Windows 7 及以上版本

### 2.2 编译环境
- MinGW GCC 编译器（支持 C++11 标准）
- Windows Sockets 2 库（ws2_32.lib）

### 2.3 网络要求
- 服务器和客户端需要在同一网络环境下，或者服务器有公网IP
- 确保防火墙允许指定端口的TCP连接

---

## 3. 编译和运行

### 3.1 编译程序

打开命令提示符，进入程序所在目录，执行以下命令：

```bash
# 编译服务器端程序
g++ -o ss.exe ss.cpp -lws2_32 -std=c++11

# 编译客户端程序
g++ -o cc.exe cc.cpp -lws2_32 -std=c++11
```

编译成功后，会生成 `ss.exe`（服务器端）和 `cc.exe`（客户端）两个可执行文件。

### 3.2 运行服务器

1. 双击运行 `ss.exe`，或者在命令行中执行：
   ```bash
   ss.exe
   ```

2. 程序会提示输入端口号，输入一个未被占用的端口号（例如：8888），然后按回车。

3. 如果显示 `bind succeed!!` 和 `listening.....`，说明服务器启动成功，正在等待客户端连接。

### 3.3 运行客户端

1. 双击运行 `cc.exe`，或者在命令行中执行：
   ```bash
   cc.exe
   ```

2. 按照提示依次输入：
   - 昵称：您在聊天室中的显示名称
   - 服务器IP地址：服务器所在计算机的IP地址（如果在本机运行，输入 `127.0.0.1`）
   - 端口号：与服务器端相同的端口号

3. 连接成功后，您将看到欢迎消息和菜单界面。

---

## 4. 连接服务器

### 4.1 连接步骤

1. **启动服务器**：
   - 运行 `ss.exe`
   - 输入端口号（如 8888）
   - 等待显示 `listening.....`

2. **启动客户端**：
   - 运行 `cc.exe`
   - 输入您的昵称
   - 输入服务器IP地址
   - 输入端口号

3. **连接成功**：
   - 显示 `connect sever succeed`
   - 收到服务器的欢迎消息
   - 显示菜单界面

### 4.2 连接示例

**服务器端：**
```
请输入服务器端口号：8888
bind succeed!!
listening.....
```

**客户端：**
```
请输入昵称
小明
请输入要连接的服务器ip地址：
127.0.0.1
请输入服务器端口号:
8888
要连接的服务器ip是 127.0.0.1    端口号：8888
本地ip：127.0.0.1   本地port:12345
connect sever succeed，输入 q to exit
[系统] 欢迎 小明 加入公共聊天室！
[系统] 输入 /menu 查看菜单，/help 查看帮助

========== 聊天室菜单 ==========
1. 创建私有聊天室
2. 加入私有聊天室
3. 离开私有聊天室
4. 查看所有私有聊天室
5. 显示帮助信息
6. 发送消息
7. 退出聊天室
================================

请选择操作 (1-7):
```

---

## 5. 菜单操作指南

连接成功后，客户端会显示菜单界面，您可以通过输入数字选择相应的操作。

### 5.1 菜单选项说明

| 选项 | 功能 | 说明 |
|------|------|------|
| 1 | 创建私有聊天室 | 创建一个新的私有聊天室，系统自动生成4位房间号 |
| 2 | 加入私有聊天室 | 输入4位房间号，加入已存在的私有聊天室 |
| 3 | 离开私有聊天室 | 离开当前的私有聊天室，回到公共聊天室 |
| 4 | 查看所有私有聊天室 | 显示当前所有存在的私有聊天室列表 |
| 5 | 显示帮助信息 | 显示所有可用的命令和功能说明 |
| 6 | 发送消息 | 向当前所在的聊天室发送消息 |
| 7 | 退出聊天室 | 断开与服务器的连接，退出程序 |

### 5.2 菜单操作示例

#### 示例1：创建私有聊天室

1. 在菜单中选择 `1`，按回车
2. 系统会创建一个私有聊天室，并返回房间号
3. 显示类似：`[系统] 私有聊天室创建成功！房间号：1234`

#### 示例2：加入私有聊天室

1. 在菜单中选择 `2`，按回车
2. 系统提示：`请输入4位房间号:`
3. 输入房间号（如 `1234`），按回车
4. 如果房间存在，显示：`[系统] 成功加入房间 1234`

#### 示例3：发送消息

1. 在菜单中选择 `6`，按回车
2. 系统提示：`请输入消息内容:`
3. 输入您想发送的消息，按回车
4. 消息会发送到当前所在的聊天室

#### 示例4：查看私有聊天室列表

1. 在菜单中选择 `4`，按回车
2. 系统会显示所有私有聊天室：
   ```
   [系统] 当前私有聊天室列表：
     房间号: 1234 (人数: 2)
     房间号: 5678 (人数: 1)
   ```

#### 示例5：退出聊天室

1. 在菜单中选择 `7`，按回车
2. 显示：`正在退出聊天室...`
3. 程序结束

---

## 6. 命令行操作指南

除了菜单操作，系统还支持命令行方式操作。您可以在发送消息时直接输入命令（以 `/` 开头）。

### 6.1 可用命令列表

| 命令 | 功能 | 示例 |
|------|------|------|
| `/create` | 创建私有聊天室 | `/create` |
| `/join <房间号>` | 加入指定的私有聊天室 | `/join 1234` |
| `/leave` | 离开当前私有聊天室 | `/leave` |
| `/list` | 查看所有私有聊天室 | `/list` |
| `/help` | 显示帮助信息 | `/help` |
| `/menu` | 显示菜单 | `/menu` |
| `q` | 退出聊天室 | `q` |

### 6.2 命令行操作示例

#### 示例1：创建私有聊天室

在消息输入界面输入：
```
/create
```
系统会创建一个私有聊天室，并返回房间号。

#### 示例2：加入私有聊天室

输入：
```
/join 1234
```
如果房间号 `1234` 存在，您将加入该房间。

#### 示例3：查看帮助信息

输入：
```
/help
```
系统会显示所有可用命令的说明。

---

## 7. 功能详细说明

### 7.1 公共聊天室

- **默认位置**：所有用户连接成功后，默认进入公共聊天室
- **消息可见性**：公共聊天室中的消息对所有在线用户可见
- **用户通知**：当有用户加入或离开公共聊天室时，其他用户会收到通知

### 7.2 私有聊天室

#### 创建私有聊天室
- 系统自动生成4位唯一的房间号
- 创建者自动加入该房间
- 房间号范围：0000-9999
- 房间号唯一，不会重复

#### 加入私有聊天室
- 需要知道正确的4位房间号
- 加入后，原所在聊天室的其他用户会收到您离开的通知
- 新房间的成员会收到您加入的通知

#### 离开私有聊天室
- 离开后自动回到公共聊天室
- 房间成员会收到您离开的通知
- 如果房间内没有其他成员，房间会自动解散

#### 消息隔离
- 私有聊天室中的消息仅对房间内成员可见
- 公共聊天室的用户无法看到私有聊天室的消息
- 不同私有聊天室之间的消息也是隔离的

### 7.3 用户状态

- **当前位置**：系统会记录您当前所在的聊天室
- **状态显示**：菜单中会显示您当前的位置（公共聊天室或私有聊天室）
- **消息路由**：发送的消息会根据您的当前位置进行路由

### 7.4 系统通知

系统会在以下情况发送通知：
- 用户加入公共聊天室
- 用户离开公共聊天室
- 用户加入私有聊天室
- 用户离开私有聊天室
- 私有聊天室创建成功
- 私有聊天室加入成功
- 私有聊天室离开成功

---

## 8. 常见问题

### Q1: 连接服务器失败怎么办？

**可能的原因和解决方法：**
1. **服务器未启动**：确保服务器端程序 `ss.exe` 已经运行
2. **IP地址错误**：检查输入的服务器IP地址是否正确
   - 如果服务器在本机运行，使用 `127.0.0.1` 或 `localhost`
   - 如果服务器在其他计算机，使用该计算机的局域网IP或公网IP
3. **端口号不匹配**：确保客户端和服务器使用相同的端口号
4. **防火墙阻止**：检查Windows防火墙是否阻止了程序的网络连接
5. **网络不通**：确保客户端和服务器在同一网络，或者服务器有公网访问权限

### Q2: 加入私有聊天室失败怎么办？

**可能的原因和解决方法：**
1. **房间号不存在**：确认房间号是否正确，或者房间是否已解散
2. **房间号格式错误**：房间号必须是4位数字，不要输入其他字符
3. **已经在该房间**：如果您已经在该房间，系统会提示您

### Q3: 如何知道自己在哪个聊天室？

**方法：**
1. 查看菜单顶部，会显示当前位置
2. 发送消息时，消息前缀会显示当前聊天室
3. 输入 `/menu` 命令，菜单会显示当前位置

### Q4: 私有聊天室会一直存在吗？

**不会。** 当私有聊天室中的所有成员都离开后，房间会自动解散。

### Q5: 可以同时在多个聊天室吗？

**不可以。** 每个用户同一时间只能在一个聊天室中（公共聊天室或某个私有聊天室）。

### Q6: 如何查看所有在线用户？

**当前版本暂不支持此功能。** 您可以通过以下方式间接了解：
- 查看公共聊天室的用户加入/离开通知
- 查看私有聊天室的成员加入/离开通知

### Q7: 消息有长度限制吗？

**有。** 单条消息最多支持255个字符（包括用户名和消息内容）。

### Q8: 程序崩溃或强制关闭会怎样？

- **服务器端**：所有连接会断开，所有聊天室数据丢失
- **客户端**：连接会断开，服务器会检测到并通知其他用户该用户已离开

### Q9: 支持中文昵称和消息吗？

**支持。** 系统使用UTF-8编码，可以正常处理中文字符。

### Q10: 如何重新连接？

如果连接断开，需要：
1. 关闭当前客户端程序
2. 重新运行 `cc.exe`
3. 重新输入连接信息

---

## 附录

### 附录A：快捷键和特殊操作

| 操作 | 说明 |
|------|------|
| `Ctrl+C` | 强制终止程序（不推荐，可能导致连接异常） |
| 输入 `q` | 正常退出聊天室 |

### 附录B：错误代码说明

| 错误代码 | 说明 |
|----------|------|
| 10054 | 连接被重置（通常是对方强制关闭连接） |
| 10061 | 连接被拒绝（服务器未启动或端口错误） |
| 10060 | 连接超时（网络不通或防火墙阻止） |

### 附录C：版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | - | 初始版本，仅支持公共聊天室 |
| 2.0 | 2026-04-14 | 新增私有聊天室功能和菜单功能 |
| 2.1 | 2026-04-14 | 修复严重bug：死锁问题、多客户端连接问题、菜单功能失效问题 |

---

## 重要说明

### 版本2.1修复内容

**重要提示：如果您使用的是2026-04-14之前编译的程序，请重新编译！**

版本2.1修复了以下严重问题：

1. **死锁问题**：修复了服务器端list类内部锁导致的死锁问题
   - 之前的问题：第一个客户端连接后，服务器死锁，无法接收第二个连接
   - 现在修复：服务器可以正常接收多个客户端连接

2. **菜单功能失效**：修复了菜单选项无法使用的问题
   - 之前的问题：除了退出选项，其他菜单选项都无法使用
   - 现在修复：所有菜单选项都可以正常使用

3. **公共聊天室消息**：修复了公共聊天室消息接收问题
   - 之前的问题：公共聊天室无法接收消息
   - 现在修复：公共聊天室消息可以正常收发

### 重新编译命令

```bash
# 编译服务器端
g++ -o ss.exe ss.cpp -lws2_32 -std=c++11

# 编译客户端
g++ -o cc.exe cc.cpp -lws2_32 -std=c++11
```

---

**感谢使用本聊天室系统！**

如果您有任何问题或建议，请随时联系开发者。
